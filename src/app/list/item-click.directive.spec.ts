import { ItemClickDirective } from './item-click.directive';

describe('ItemClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemClickDirective();
    expect(directive).toBeTruthy();
  });
});
